<Window x:Class="IEC101MasterTester.Views.LineMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IEC-101 Line Monitor"
        Height="640"
        Width="1240"
        MinHeight="360"
        MinWidth="900"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SharedUi/ModernTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10" Background="#0F172A">
        <Border Background="#111827" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="8">
        <DataGrid ItemsSource="{Binding LineMonitor}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  ClipboardCopyMode="IncludeHeader"
                  GridLinesVisibility="Horizontal"
                  Background="#111827"
                  Foreground="#F8FAFC"
                  BorderBrush="#334155"
                  HorizontalGridLinesBrush="#1F2937"
                  VerticalGridLinesBrush="#1F2937"
                  RowBackground="#111827"
                  AlternatingRowBackground="#0F172A"
                  RowHeaderWidth="0"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#17202B" />
                    <Setter Property="Foreground" Value="#CBD5E1" />
                    <Setter Property="BorderBrush" Value="#334155" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderBrush" Value="#1F2937" />
                    <Setter Property="Foreground" Value="#E2E8F0" />
                    <Setter Property="Background" Value="Transparent" />
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#111827" />
                    <Setter Property="Foreground" Value="#E2E8F0" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#1E293B" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="95" />
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="70" />
                <DataGridTextColumn Header="Frame Type" Binding="{Binding FrameType}" Width="95" />
                <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="240" />
                <DataGridTextColumn Header="Control / FC" Binding="{Binding ControlFc}" Width="250" />
                <DataGridTextColumn Header="DFC" Binding="{Binding DFC}" Width="50" />
                <DataGridTextColumn Header="ASDU Type" Binding="{Binding AsduType}" Width="130" />
                <DataGridTextColumn Header="COT" Binding="{Binding COT}" Width="120" />
                <DataGridTextColumn Header="CASDU" Binding="{Binding CASDU}" Width="70" />
                <DataGridTextColumn Header="Raw Hex / Detail" Binding="{Binding RawHex}" Width="360" />
            </DataGrid.Columns>
        </DataGrid>
        </Border>
    </Grid>
</Window>
